/**
 * features/member-app/types/index.ts
 *
 * Module-local alias for the canonical types in `types/member-app.ts`.
 * Components inside this feature import from here so the canonical file can be
 * relocated without touching every component.
 */

export type * from '@/types/member-app'
