import { BarChart3 } from 'lucide-react'
import { FoundationPage } from '@/components/ui/FoundationPage'

export default function ProgressPage() {
  return <FoundationPage title="Progress" description="Measurements, progress photos, and accessible charts arrive in the progress milestone." icon={BarChart3} accentClass="bg-cyan-50 text-cyan-700" />
}
