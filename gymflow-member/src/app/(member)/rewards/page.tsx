import { Trophy } from 'lucide-react'
import { FoundationPage } from '@/components/ui/FoundationPage'

export default function RewardsPage() {
  return <FoundationPage title="Rewards" description="XP, badges, streaks, challenges, and gym-scoped leaderboards arrive in the gamification milestone." icon={Trophy} accentClass="bg-amber-50 text-amber-700" />
}
